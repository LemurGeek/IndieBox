<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    tools:context=".MainMenu"
    android:background="@drawable/gradient_list"
    android:id="@+id/mainLayout"
    >




    <LinearLayout
        android:id="@+id/ll_gamesList"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical" >
        <ImageView
            android:id="@+id/topbar"
            android:layout_width="wrap_content"
            android:layout_height="69dp"
            app:srcCompat="@drawable/top_bar" />

        <ImageView
            android:id="@+id/btGotoGames"
            android:layout_width="348dp"
            android:layout_gravity="center"
            android:layout_height="207dp"
            android:clickable="true"
            app:srcCompat="@drawable/games_menu_button" />

        <ImageView
            android:id="@+id/btGotoApps"
            android:layout_width="348dp"
            android:layout_gravity="center"
            android:layout_height="207dp"
            android:clickable="true"
            app:srcCompat="@drawable/apps_menu_button" />

        <ImageView
            android:id="@+id/btGotoRegister"
            android:layout_width="348dp"
            android:layout_height="207dp"
            android:layout_gravity="center"
            android:clickable="true"
            app:srcCompat="@drawable/register_button_menu" />
    </LinearLayout>

    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/btLogOut"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|right"
        android:layout_margin="10dp"
        android:clickable="true"
        app:srcCompat="@drawable/ic_logout" />

</androidx.coordinatorlayout.widget.CoordinatorLayout>